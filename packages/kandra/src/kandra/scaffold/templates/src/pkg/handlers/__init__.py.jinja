"""Command handlers.

Each handler class declares a frozen-dataclass ``request`` and
``response`` type pair. The kandra build step introspects those types
and emits typed Python wrappers in the generated client.
"""
