"""{{ display_name }} — user-authored device modules.

This package contains the source kandra reads to introspect handler types
and generate the SDK. The generated client lives in ``dist/`` after you run
``kandra build kandra.yaml``.
"""
